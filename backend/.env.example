# Local: docker compose up -d  then use URL below
DATABASE_URL="postgresql://clinic:clinic_secret@localhost:5432/homeopathy_clinic"

# Production: Supabase → Connect → Prisma → copy DIRECT_URL (port 5432)
# Encode password special chars: @ → %40, # → %23
# DATABASE_URL="postgresql://postgres.[ref]:Pass%40123@....pooler.supabase.com:5432/postgres"

JWT_SECRET="change-me-in-production"
PORT=4000
FRONTEND_URL=http://localhost:5173
UPLOAD_DIR=./uploads
WHATSAPP_ENABLED=false

# Optional — patient report images (else stored in backend/uploads/)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
