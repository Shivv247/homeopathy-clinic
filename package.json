{
  "name": "homeopathy-clinic",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "description": "Full-stack Clinic Management App for Indian Homeopathic Doctors",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run setup --prefix backend",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix backend",
    "start": "NODE_ENV=production npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:seed": "npm run db:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
